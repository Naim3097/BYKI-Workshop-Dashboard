{
  "name": "workshop-commerce-platform",
  "private": true,
  "version": "1.0.0",
  "description": "BYKI-ready standardized platform for car-workshop landing + diagnose + commerce sites",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.8.3"
  }
}
